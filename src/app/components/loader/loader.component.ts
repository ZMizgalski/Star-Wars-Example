import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'web-loader',
  template: `<div class="loader-con">
    <div class="loader">Loading...</div>
  </div>`,
  styleUrls: ['./loader.component.css'],
})
export class LoaderComponent {}
